# JUDGMENT_LOG

Claude（AI）の提案と、PdMの判断・採用を時系列で記録する。意思決定の質と介在価値を可視化する目的。
※顧客固有データ（社名・売上・利用実数）は記録しない。判断プロセスのみ。

---

## 2026-05-26 — データ駆動の顧客優先度づけ（スナップショット機能の導入対象選定）

文脈: SaaS経営管理プロダクトで、ある機能のステージング検証対象顧客を、データから優先順位づけする。

| # | Claude案 | PdMの判断・採用 | 学び/介在価値 |
|---|---|---|---|
| J1 | 本番DBへ読み取り専用SQLを用意して実行依頼 | 本番に触れず、既存BI(Redash)のキャッシュ結果をエクスポートして取得（本番無負荷）。本番環境保護のガードレールを堅持 | 「正しいデータの取り方」を本番リスクで判断。手段の安全性を優先 |
| J2 | 初期の優先度は「CSが個社レポートを何本作ったか」=CS工数の代理指標で提示 | 「それはヘルススコアから算出されたものか?」と根拠を追及→実ヘルス指標+利用ログで再算出に格上げさせた | 代理指標と実測の差を見抜く。根拠の質を要求 |
| J3 | 利用ログ(操作PV)で優先度を再評価 | 「アプリとWebの区別ができていないのでは?」と計測の妥当性を指摘→ログが管理Web操作のみ(モバイル/API不可視)と判明、データ量は別指標で補完するよう修正 | 計測の前提(取得チャネル)の妥当性を疑う。データリテラシー |
| J4 | 恩恵軸(利用量)と検証軸(機能の正しさ)を混在させて提示 | 「操作PVの多い順で優先度を作るべきでは」と主軸を提案→恩恵軸=操作PV主軸、検証軸=別枠、の二段構えに整理させた | 意思決定の軸を1本に定める判断。優先順位の設計 |
| J5 | 成果物をリポジトリにPUSH | 顧客データを含むため公開リポジトリを禁止→privateリポジトリで管理と判断 | データガバナンスの即断。漏洩リスクの予防 |
| J6 | 専門用語(SCD/凍結/中間テーブル等)で仕様記述 | PO/エンジニアに伝わらないと指摘→「写真のたとえ」等やさしい表現に統一、出典([ログ]/[ヘルス])を全指標に明示させた | 受け手別の翻訳。伝達設計 |
| J7 | 単機能(スナップショット)の仕様に集中 | CSクエリ横断で機能化候補を棚卸し→さらに「従来UI仕様 vs AIエージェント・レール設計」で仕分けする視点を要求 | スコープを点から面へ。アーキテクチャ視点 |

総括: PdMは「AIに分析を実行させ、自らは根拠・計測・軸・統治・伝達を検証/設計する」エディタ兼アーキテクト型の介在。AIの出力を鵜呑みにせず、各段で前提を疑い品質を引き上げた。
